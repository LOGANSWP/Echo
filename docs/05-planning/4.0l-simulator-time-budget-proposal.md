# 4.0l 模拟器诗歌生成时间预算增补提案

状态：用户已批准（原文：“批准”）；2026-09-09T20:19:30.747294+00:00。已实施并更新安装 16 Pro。预算与相关契约测试通过；同图诗歌仍超时，功能验收未通过。

## 已验证的问题

iPhone 16 Pro / iOS 18.2 的真实照片理解已完成，中文诗歌仍触发单调用 60 秒上限。精简提示词后，加载 1.72 秒、prefill 12.43 秒、decode 37.34 秒、选词 8.94 秒，总计 60.65 秒，已产生 104 个 token，但 envelope 未完成。没有将部分结果或失败伪装为成功。

## 请求批准的精确范围

- 仅 `DEBUG && targetEnvironment(simulator)` 的手动创作：单次生成模型调用上限由 60 秒改为 120 秒。
- 同一手动创作请求的总截止由 120 秒改为 240 秒，包含既有最多一次语言重试；不新增重试。
- 实机、Release、自动月报/年报预算保持现状；模拟器结果不能替代实机性能或发布门禁。
- 模型、tokenizer、模型工件、计算单元、1024 上下文、768 输入、256 输出 token、1.5 GB 内存与来源/语言校验保持不变。
- 不增加下载或缓存副本，不改变照片按需理解规则。诊断日志仅记录阶段耗时和 token 数量。

## 批准后的具体实现与验证

在运行时与手动创作请求构造处读取同一编译条件预算配置；运行时仍取单调用限制与请求剩余时间的较小值。为模拟器/生产预算边界补定向测试；重新构建并安装 16 Pro，以同一张照片验证四行中文、来源锚点和实际耗时。若仍失败，保留真实结果，不再自动延长上限。

本方案只是让模拟器功能验证有足够执行时间，不声称解决底层推理性能。

## 审批依据

`docs/05-planning/4.0k-approval-packet/README.md` 原文：
> 输出最多 256 tokens；60 秒截止；正文/JSON 不修复；每次最多一次语言重试

原始冻结审批包保持不变，本文件单独记录增补范围。


## 实施结果

`GenerationExecutionScope` 在 Debug 模拟器手动创作中传递 120/240 秒预算，语言重试继承 scope 与原截止。标准 scope 继续 60 秒/调用，实际自动报告调用不设置 manualCreation。新增边界测试与回归 19 tests/2 suites 通过。16 Pro 最终同图验证 elapsed=120.245s，prefill=94.305s，75 output tokens 后 deadline；当前仍为失败，未再增加时限。详情见 `4.0l-poem-timeout-evidence.json`。
