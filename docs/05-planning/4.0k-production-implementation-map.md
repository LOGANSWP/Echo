# 4.0k 生产接线与测试落点

**状态：确切工件已获批，生产实现与验证进行中。** 下表“当前代码事实”保留审批前基线，不能用于描述当前工作区；已落地进展与尚未通过的证据见末尾。不改变4.0j调度/存储已经交付的范围。

按ADR-023先取得具体工件审批，再装配生产。以下顺序在同一4.0k任务内执行，不另外拆任务或跳过依赖。

| 边界 | 当前代码事实 | 实现与测试要求 |
|---|---|---|
| 工件可用性 | `LiveAppAdapters.resolveLLMProvider()`恒为nil；`makeCreativePipeline`返回optional | 根据确切审批清单与完整文件校验构建provider。未装配保持generationUnavailable；已配置缺失/损坏/未批准工件返回L3，在报告claim前体现，不能都变为nil或L2 |
| Typed runtime | `LLMProvider.generate(prompt:preferredLanguage:)`仅返回String，无来源、预算、trace、finish reason | 添加Sendable请求/结果/预算/错误/身份类型。携带trace及当前语言、实际来源allow-list、token上下文和终止原因；不跨Actor传业务任意闭包 |
| Core ML隔离 | 原生研究CLI证明单所有者可用，尚无生产Actor | Actor独占模型和每请求MLState；跨await仍需同一请求所有权，禁止把Actor可重入当锁。同步初始化，合法异步工厂/装载；取消/撤权后释放，严格并发检查 |
| 语言对齐 | `LanguageAligner.align`在解析前识别整段JSON，重试复用同一prompt，返回String fallback | 有界envelope先解析，再检测正文/简中脚本；每调用最多一次更严格语言重试并重新算token，schema不修复。typed fallback不能进入publication；持久UserPolicy按请求重读 |
| 创作输出 | `CreativePipeline.generate`先align后parse，广泛catch可能吞掉取消/模型错误 | 重排结构与语言验证，保留NoSource/partialNoSource；显式传播取消/denial/L3，避免错误变成成功fallback。保持现有typed分享/引用边界 |
| 真分层 | `CreativeNarrativeReportGenerator`把`request.sourceBatches`通过`request.sources`拍平成一次调用，并照搬计划coverage | 实际逐批调用；reduce只消费cited子段落，逐层allow-list只含提交子段落携带的叶子来源；限制fan-in/层数/总调用/时间；实际coverage与贡献来源从调用记录累计 |
| 发布与D-005 | `NarrativeReportActor`用`prepared.sources`发布，已有无挂起点数据库事务 | 发布依赖覆盖所有实际贡献输入，而不限于最终可见anchor；publication前按当前策略/来源版本重验。沿用已有事务、周期CAS与删除联动，不能新建第二套领域状态 |
| 恢复 | 入队执行仍在generator前报告index0，生成后直接跳到prepared.sources.count | 版本化resume descriptor包含身份/算法/限制与checkpoint，不含原文/草稿/KV；Continue保留原checkpoint并重建缺失内存前缀；重放计预算，身份变化走合法Restart |
| 审计 | SYN-001 AC-6要求`outputLanguage/uiLanguage/languageRetryCount`，当前Core未找到相应存储字段 | 按专用枚举/整数列补齐迁移与PrivacyActor typed payload，更新允许字段规约；不得将文本、prompt或模型错误原文写入审计 |
| 默认依赖图 | 创作与报告共用composition，当前无真实provider | 注入同一合法runtime与限额策略，报告扫描先读取运行时状态，再claim；单元测试可替身但生产no-fixture必须真实获批模型 |

正式测试落在`EchoTests/Phase4/4.0k_OfflineGenerationRuntimeTests.swift`及必要的独立集成文件。回归4.0i引用/分享、4.0j调度/发布、4.0g恢复，以及相关3F.9/3F.10语言和创作测试。使用iPhone17Pro、串行测试；不将本任务测试冒充Phase4正式集成任务。

测试先覆盖缺失/损坏/未批准工件零预测、完整token预算、EOS/截断/取消、并发请求不共享state；再覆盖一次语言重试、逐层来源/coverage、prefix replay、发布前撤权/删除、L2仅手动重试和资源延后。最终真实模型E2E使用通过生产摄入的无PII合成记忆，禁止fixture、Stub或普通文本fallback充当报告成功。

当前研究素材和预算检查可作为输入及对照，不能直接复制“通过”状态到这些生产测试。原生代码迁入Echo时必须适配项目`SWIFT_DEFAULT_ACTOR_ISOLATION=MainActor`与现有PrivacyCheckpoint/错误矩阵；CLI编译成功不证明App编译成功。


## 2026-09-08T15:41:59Z 工件使用批准

用户在当前任务明确回复“批准”，授权固定审批包 `0e202c15169faf241d6ca77fa905493c7d3e57ba1e27f8df2d67c4fc58f136a7` 的 Qwen3-0.6B context1024/int8 per-channel 工件、原生运行时集成方案及 ≤650MB 生成模块预算。审批记录见 `4.0k-approval-packet/approval.json`。48 份冻结附件及 10 个候选资源重新校验一致；进入生产实现。此决定不代表质量、实机资源或发布门禁已经通过。

## 实现与验证进展（2026-09-08）

- 默认 AppComposition 已装配同一 BundledGenerationActor，生产 Creation 与报告 generator 消费 typed 请求；原生 BPE、完整资源哈希、结构化语法、单请求状态与输出预算已进入 App。语言校验先解析 envelope，真实 sourceBatches 经 leaf/reduce 调用，贡献来源与恢复身份随请求传递。
- `python3 Scripts/prepare_generation_resources.py --verify-only` 复核冻结审批附件及 11 个 App 资源，合计 609,923,570 字节。安装脚本只读取本地固定工件，拒绝覆盖不匹配资源；模型二进制不提交 Git。
- 4.0k 与 4.0g 合计 24 个测试通过（`/tmp/echo-4.0k-blocking-green.log`）；包含 L3 恢复失败不写 PendingOperations、不重置 checkpoint。此计数仅为当前切片，不代表完整覆盖率或质量门禁。
- 真实 E5 生产摄入到 canonical memory 已进入集成流程；Qwen 模型在 iOS 26.5 模拟器 Core ML 加载时报 MPSGraph/MIL 转换错误并返回 L3，真实生成集成测试仍失败。Mac 原生研究结果不能替代该失败。
- 已连接的 iPhone 14 Plus / iOS 18.2.1、Developer Mode 已核验。Release 实机测试被开发签名阻断：Apple 返回 `com.echo.Echo` 无法注册到当前团队。等待可用开发 Bundle ID 或修复现有签名；未修改项目签名配置，未获得实机推理、取消、内存或质量通过证据。
- 后续仍需完成报告晚期 L3/资源分级、最终来源校验、运行时登记、完整回归与真实设备验收；任务保持 in_progress，DEF-78-001 保持 open。

### 模拟器后端诊断与限定修正

2026-09-08 在同一固定模型上比较 Core ML 配置：模拟器 `.cpuAndGPU` 加载失败；独立 iOS Simulator 可执行诊断使用 `.cpuOnly`，加载并完成三次真实 MLState 预测，logits 维度 151,936，状态 56 个。诊断二进制的 LC_BUILD_VERSION 为 IOSSIMULATOR / minOS18 / SDK26.5。这支持仅在 `targetEnvironment(simulator)` 下选择 CPU-only；实机继续固定 `.cpuAndGPU`，不修改工件、tokenizer、语言、调用或资源预算。后端名称纳入恢复执行身份。模拟器结果仅验证功能，不作为实机时延、内存或质量证据。

### 工程生成失败保留

- 固定开发摄入样本在 Debug/CPU-only 上触及 60 秒限额；原来的通用 fallback 包装已修复，预算错误现在返回类型化失败。
- 优化 Debug（全模块优化）在同一样本上运行 53.010 秒，达到 256 输出 token 后仍未 EOS，返回 outputLimit，没有成功创作或报告。这是开发样本反馈，不修改任何预算或质量分母。
- 纯 Release 测试编译受既有 DEBUG-only fault API 阻断；启用 testability 后仍缺少该 API。后续优化 Debug 只作功能证据，不作为 Release 工程包资源验收。

### CPU-only 假设撤回与生产提示对照（2026-09-08）

上面的三次预测只证明可执行，不能证明输出正确。后续固定 45-token 前缀、逻辑 MLMultiArray 索引对照中，模拟器 CPU-only 与 Mac CPU-only 都只有 2/8 个 top-1 匹配参考；抽样 logits 最大误差分别约 26.36、26.67。模拟器完整生成出现大量空白与重复标点并耗尽 256-token 输出。因此已撤回 CPU-only 兼容实现，生产保持获批的 `.cpuAndGPU`，不以错误后端掩盖模拟器 L3。

使用同一编译工件和生产 `GenerationTokenizer`、`GenerationEnvelopeGrammar`、`GenerationBudget` 的 Mac CPU/GPU 诊断，`observations-production-v3` 完整合成提示以 EOS 结束：306 次预测、7.116 秒、合法单段 JSON、来源 ID 属于实际输入。v3 明确单段及 45 英文词/70 中文字符输出目标，完整模板与语言重试仍计入 768+256 token 上限。此项只定位后端差异，不是 App/iPhone E2E，也不是冻结 400 例质量通过证据。

日志：`/tmp/echo-4.0k-mac-cpu-reference.json`、`/tmp/echo-4.0k-simulator-reference.json`、`/tmp/echo-4.0k-simulator-diagnostic.jsonl`、`/tmp/echo-4.0k-mac-gpu-generation.jsonl`。诊断仅使用无 PII 合成文本，不生成媒体。实机签名问题仍待解决。

## 2026-09-08 simulator-first progress

The user requested simulator completion before physical devices. A separately identified RMS-FP32 graph fixed CPU inference while preserving the exact approved quantized weights. Real E5 ingestion, default composition, creation and monthly/yearly publication now pass on iPhone 17 Pro / iOS 26.5. Device and Release builds retain the original artifact. See `4.0k-production-validation.md` for evidence and remaining gates.
