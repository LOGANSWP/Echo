# 4.0k 下一候选研究下载请求

**状态：等待明确下载授权；未下载、未装配、不代表质量或产品使用批准。**

## 确切请求

- 官方仓库：[Qwen/Qwen3.5-0.8B](https://huggingface.co/Qwen/Qwen3.5-0.8B)。模型卡将其定位于原型、任务微调与研发用途，不能由模型卡推断 Echo 质量通过。
- 固定版本：`2fc06364715b967f1860aea9cf38778875588b17`。
- 官方清单：13 个文件，合计 **1,769,980,465 bytes（约 1.77 GB）**。
- 权重文件：`model.safetensors-00001-of-00001.safetensors`，1,746,942,600 bytes，SHA-256 `04b1c301231dd422b8860db31311ab2721511346a32cb1e079c4c4e5f1fe4696`。
- 每文件 size、上游 Git blob ID / LFS SHA-256 已记录于 [研究证据 JSON 的 nextCandidate.files](4.0k-poem-grounding-followup-results.json)。固定版本下载后逐项核对，禁止静默更新 hash 基线。
- 目标目录：`PinnedModels/offline-generation-evaluation/qwen3.5-0.8b/source/`，已由既有规则排除 Git；现在不存在。

## 授权后的工作

仅下载公开工件到开发机，使用无 PII 合成资料进行本地离线生成对照。先检查既有 1024 上下文、768 输入、256 输出预算内的协议、双语和来源质量，再决定是否值得进行 Core ML/量化工程评估。沿用已安装的研究工具，不执行仓库远程自定义代码，不使用云端推理。

不同架构、tokenizer、量化后的质量、Core ML iOS 18 兼容性、650 MB 模块大小和设备资源均尚未验证。源权重大小不等于安装包大小；不能用理论位宽估算宣称包体门禁通过。若进一步研究得到合格确切工件，仍需按 ADR-023 提供可审阅的完整工件材料后申请 App 使用批准。

本次下载授权不包含替换 App 模型、提高现有运行时预算、修改签名、发布或合并。原 Qwen3-0.6B 的既有批准保持有效。

## 自动审批拒绝

自动审批认为新增约 1.76 GB 外部模型的磁盘占用与任务范围影响较大，而用户的“继续模拟器修复”没有明确授权这份新下载。因此该操作未执行，未通过其他工具或路径绕过。继续上述确切下载需要用户明确批准。
# 当前安排：用户于 2026-09-08 明确先打通功能流程，模型内容质量研究暂缓。本文保留为历史下载提议，不再是当前功能工作的待批准前置条件；没有下载或模型替换授权。
