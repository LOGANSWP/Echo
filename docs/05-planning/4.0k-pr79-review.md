# PR #79 architecture review — Task 4.0k

**Reviewed head:** `31d2c7969f5e7669c0b74aec3f6d58cdb80ca0db`
**PR base:** `dev-1.0` / `dc6e97298d80493c43565cf88a35ca0b2e837506`
**Result:** Changes required; do not merge yet.
**Scope:** 206 added/modified files (28 Core files, 32 Swift/Python test files, 146 other files). Pure synthetic datasets, generated evaluation data and frozen license resources were excluded from line-by-line business-logic review. All changed Core actor/pipeline files were inspected; services, UI/composition, build/resource preparation and AC evidence were reviewed around those boundaries. This is a manual architecture review, not a CodeRabbit review or exhaustive formal verification.

## Findings

### R79-1 / P1 — Required CI does not provision the newly mandatory generation resources

- **Location:** `Echo/App/AppComposition.swift:141-145`; `EchoTests/Phase4/4.0k_BundledGenerationIntegrationTests.swift:19`; `Scripts/prepare_generation_resources.py:69-79`.
- **Trigger:** A fresh CI checkout runs the required full test lane. AppComposition now always creates the real provider from `OfflineGenerationSimulator.bundle` in Debug simulator builds, but the bundle and its pinned sources are intentionally excluded from Git. The existing CI preparation invokes only `Scripts/prepare_models.sh`; it neither materializes nor verifies the new generation bundle. The added local preparation scripts require existing pinned files and are not invoked by CI.
- **Evidence:** [CI run 34301760595](https://github.com/logan-suu/Echo/actions/runs/34301760595), `Build, Lint & Test`, failed. The run executed 1,410 tests / 170 suites and reported 11 issues: **six new `invalidArtifact` issues**, plus five recorded historical known issues. Both real ingestion/report cases and all four real poem cases failed before generation. The model-quality assertions were not reached in these six CI cases.
- **Impact:** The required lane fails on a clean checkout and cannot validate the real production generation graph. Local selected-test success does not establish a reproducible CI or packaged artifact.
- **Fix:** Provide a controlled build-time source of the exact approved pinned artifact, materialize and verify the appropriate bundle before Xcode builds, and include artifact/preparation identity in any relevant cache key. Keep model binaries out of Git and prohibit App runtime downloads. Do not skip the six tests, swap in a fixture, or weaken checksum/approval gates. Re-run the required lane and address any subsequently exposed failures.
- **Rules:** AGENTS.md §3.4, §9.3, §15.2; ADR-023 exact-artifact and no-fixture acceptance.
- **Tracking:** `DEF-79-002`, open, current PR before merge. No CI configuration or resource distribution change was made by this review.

### R79-2 / P2 — Reject oversized manual inputs before rendering the entire prompt

- **Location:** `Echo/Core/Services/StructuredGeneration.swift:50-56`, with the unbounded source load at `CreativePipeline.swift:346-369`.
- **Trigger:** Select a long imported note or a photo with a large persisted description. Manual generation only checks the number of source IDs (24), loads full effective text and passes it into the prompt builder. JSON encoding, JSON-to-String conversion, source joining and final prompt interpolation happen before the tokenizer's 16,384-byte input check. The 512-scalar report-excerpt path does not protect manual creation.
- **Evidence:** A temporary standalone Swift probe compiled the actual production `GenerationRequest`, `GenerationReferenceMap` and `StructuredGeneration` files with minimal surrounding type stubs, without changing repository code or running a model. A synthetic 1,000,000-byte source produced a fully rendered 1,000,333-byte user prompt with no construction-time rejection. This demonstrates late input bounding; it does **not** claim a measured jetsam/crash or real-model failure.
- **Impact:** Large stored text is copied/encoded repeatedly before the request is rejected, producing avoidable memory pressure and potentially exhausting memory before the runtime limit can protect the App.
- **Fix:** Add an early bounded source-read/aggregate-byte guard before encoding, joining or escaping text; retain exact tokenizer checks for the complete rendered prompt and output reservation. If rejecting oversized manual input, use the existing context-limit error without silently truncating source facts. Cover one oversized source, multiple sources exceeding the aggregate limit and JSON escaping expansion; verify rejection before serialization/inference.
- **Rules:** AGENTS.md §4.1/§4.3; ADR-023 §2 bounded source/input/resident memory; US-SYN-004 input-budget principle as shared runtime contract.
- **Tracking:** `DEF-79-003`, open, current PR before merge. Production code remains unchanged in this review.

## Existing acceptance limits — not new findings

- `DEF-78-001` remains open for full Task 4.0k qualification. The last dedicated local factual-grounding run still failed the rain/night and river/birdsong assertions. Formal quality percentages, physical-device/Release resources and full coverage acceptance are not passed. User prioritization of functionality does not establish those results.
- `DEF-79-001` / Task 4.0l tracks automatic photo understanding and no-manual-description creation. The manually described blue-circle journeys prove text-input generation/export only. ADR-025 correctly marks the new photo ACs unimplemented; they are not falsely marked complete in the current PR.
- The PR is Ready for review at the user's request. That UI state is not approval of any acceptance gate; no merge was performed.

## CodeRabbit comment adjudication

There are **zero inline CodeRabbit review comments** to accept or reject. Its issue comment states that review was **skipped** because this PR has 206 files versus its 100-file limit and insufficient credits/metered capacity. GitHub's successful CodeRabbit status therefore is not evidence of a completed code review. No billing change, new CLI review, comment-command execution or model download was attempted.

## Contract review notes

- Exact resource inventory/hash checks, explicit request-owned model state and grammar/source-alias decoding are present. The current actor ownership flag protects concurrent model requests across suspension points.
- Report publication checks the claim, persisted consent/policy and all contributor source revisions/digests inside the database transaction. This closes the final cross-actor publication race for the production envelope.
- Per-layer generation restricts source IDs to actual consumed input; reductions exclude uncited/partially cited intermediate paragraphs and retain contributor dependencies separately from visible anchors.
- Structured body-language validation and a single retry are implemented; formal language/quality success rates remain unqualified. Input preflight needs R79-2, so budget ACs should not be labeled entirely passed.
- Recovery preserves checkpoint identity and rebuilds lost intermediate content; source/model/prompt/language identity changes require Restart. This review did not repeat real-model recovery timing tests.
- Copy/Markdown/PDF now format title/body only. Internal provenance and pre-export authorization remain in the domain flow. The existing clean-export tests and live records are correctly scoped; no new UI/media capture was made.
- PR AC rows sometimes use suite names rather than literal filenames; the referenced existing suites resolve to changed test files. Rows explicitly labeled inherited foundation/partial are not treated as newly implemented ACs. No new false full-quality claim was found; R79-1 and R79-2 require updated status.

## Validation performed for this review

- Verified PR head/base and paginated all 206 changed files. GitHub rejected `gh pr diff` above its 20,000-line limit; local comparison used the exact verified base/head.
- Read required CI failure logs, review records and all available CodeRabbit comments.
- Compiled and ran the isolated production-prompt construction probe using synthetic text. It does not substitute for integration tests.
- Documentation/finding JSON and whitespace checks are performed with the review-record update. No App tests were rerun solely to reproduce the already explicit missing-artifact CI failure.

**Conclusion:** ❌ Review not passed. Fix R79-1 and R79-2, then rerun relevant checks. Retain all previously declared quality/device/coverage and photo-input boundaries; do not infer merge readiness from local functional counts or CodeRabbit status.
