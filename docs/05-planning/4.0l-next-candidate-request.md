# 4.0l 下一候选：Qwen3.5-0.8B 研究请求

**状态：2026-09-09 因用户要求功能闭环优先而延后；未获下载授权、未下载，不是当前待批准或推进前提。**

SmolVLM-256M 在已批准研究范围内完成筛选，中文描述与部分可见内容处理失败，源文件已清理。此处保留原后备提议，当前继续已授权的 SmolVLM 原生功能研究；未来质量优化时可再评估官方 [Qwen/Qwen3.5-0.8B](https://huggingface.co/Qwen/Qwen3.5-0.8B)：模型包含视觉编码器，发布方提供多语言与视觉评测；这只能支持候选选择，不能当作 Echo 的双语/资源通过证据。

## 确切请求

- 固定 revision：`2fc06364715b967f1860aea9cf38778875588b17`。
- 清单：[4.0l-candidate-inventory.json](4.0l-candidate-inventory.json) 中 `Qwen/Qwen3.5-0.8B` 的 13 个根文件。
- 合计 **1,769,980,465 bytes（约 1.77 GB）**。
- 权重：`model.safetensors-00001-of-00001.safetensors`，1,746,942,600 bytes，LFS SHA-256 `04b1c301231dd422b8860db31311ab2721511346a32cb1e079c4c4e5f1fe4696`。
- 目标：`PinnedModels/photo-understanding-evaluation/qwen3.5-0.8b/source/`；Git 忽略、固定身份核对、拒绝覆盖、临时下载及时清理。
- 首先使用现有工具做本地无 PII 图像的双语/画面描述、token、EOS/取消及 host 资源筛选；不执行仓库远程代码，不访问个人照片，不使用云端推理。
- 沿用单候选 **3GB 总研究磁盘上限**、8GB host 进程限额和单实验 30 分钟。源文件已占约 1.77GB，仅剩约 1.23GB 给其他产物；完整 FP16 导出很可能无法同时保留，转换前必须先计算临时空间并安排本任务产物清理。无法在限额内完成则报告，不能自动扩容或删除其他任务文件。
- 首轮单图使用有界分辨率（拟议 256×256，实际按固定 processor 校验）、上下文 1024、完整输入最多 768 token、输出最多 128 token、单描述 60 秒。限额不足只报告，不扩大原 App 预算。

仅当源模型筛选可用，才继续在上述空间范围内设计转换/数值验证；混合 linear/full attention、视觉位置编码和新的 tokenizer 必须单独适配，现有 Qwen3 文本运行时不能直接作为兼容实现。任何新工具安装、增大磁盘预算或 App 装配须另有确切范围与授权。

此请求与历史 4.0k 的诗歌质量候选提议分别追踪；本次目的是 ADR-025 的照片直接创作输入。SmolVLM 的下载批准不自动扩张到这份更大工件。最终生产工件仍需完整转换 hash、许可/NOTICE/SBOM、冻结资源/质量材料后独立批准。
