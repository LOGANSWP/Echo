# 4.0l 有界视觉理解研究下载请求

**后续原生功能检查点已完成**：同一候选已转换并由 Swift/Core ML 从两张受控图片生成完整描述。见[功能研究复核](4.0l-functional-research-review.md)。以下保留各阶段事实，不以历史“未执行”覆盖该新证据。

**状态：2026-09-09 用户已批准本研究范围，并要求注意磁盘、及时删除不需要的文件。生产模型批准仍为 pending。**

本请求已执行源校验与 27 次参考推理。此前约 518MB 源文件已清理；用户随后明确功能闭环优先，现继续原授权内的固定候选转换/原生研究。质量问题保留，继续工程研究无需重复批准相同范围。结果见 [研究复核](4.0l-screening-review.md)。

建议先下载 [HuggingFaceTB/SmolVLM-256M-Instruct](https://huggingface.co/HuggingFaceTB/SmolVLM-256M-Instruct)，以真实图片输入验证视觉描述与 Core ML 可行性。选择理由和后备候选见 [评估](4.0l-photo-understanding-evaluation.md)。本轮不下载 SmolVLM-500M、Qwen3.5 或 FastVLM。

## 确切范围

- 固定 revision：`7e3e67edbbed1bf9888184d9df282b700a323964`。
- [候选清单](4.0l-candidate-inventory.json)本地 SHA-256：`d341144801c6dc27a08b959c796dad69d41b08b470184171786c90a217c82d72`。其中仅 `HuggingFaceTB/SmolVLM-256M-Instruct` 条目属于本请求；其他条目只供比较。
- 文件：候选清单中该仓库 `rootFiles` 的 14 个文件；共 **517,904,565 bytes（约 518 MB）**。ONNX 等子目录不在范围内。
- 权重：`model.safetensors`，513,028,808 bytes，上游 LFS SHA-256：`74dea5904032e5ae99a2e0eef5179e6ac0f1dedc3ab0c7c2a5d4d387c843203e`。
- 目录：`PinnedModels/photo-understanding-evaluation/smolvlm-256m/source/`；须确认被 Git 忽略，独立创建并拒绝覆盖已有不匹配文件。
- 下载后逐文件验证固定身份，生成本地完整 SHA-256 清单。不得将任何 binary 提交 Git。
- 仅在开发机研究，最多新增 3GB 磁盘，单研究进程峰值 8GB、单实验 30 分钟；沿用评估文档的图像、token、调用与时间限额。超过即停止并报告。

授权后的步骤：固定源校验 → 无 PII 内存测试图的 PyTorch 描述与双语筛选 → Core ML 转换/数值对照 → Swift 原生研究 CLI → 汇总失败与可行性证据。工具限于已有安装；不执行模型仓库远程代码，不联网推理，不访问个人照片，不保存截图/视频，不自动切换更大候选。

这份请求仅取得真实候选以完成下一阶段研究材料，不授予 App Bundle/生产 composition 装配、模型替换、签名、分发、发布或门禁修改。转换产物、完整运行时/许可/SBOM 和设备预算尚不存在，不能现在把未知产物批准为生产模型。

## 为什么需要这一项授权

用户的“开始执行”已授权任务启动、候选调查和计划材料。ADR-025 明确规定：

> 本决定只授权文档与任务规划，不批准新模型下载、权重替换、视觉模型工件、签名或发布。

任务账本同时要求先形成材料、再取得确切工件审批。故此处请求的是上面列明的单个研究下载与实验范围，而不是重新询问是否开始 4.0l。本段记录最初请求授权的原因；后续固定 SmolVLM 下载已获用户批准并完成，不是当前待确认事项。

## 研究授权记录

用户回复：“确认，但需注意磁盘空间占用，不需要的及时删除”。本次研究按 3GB 上限执行，并及时清理本次产生的临时下载和不再需要的失败转换产物；固定源与精简证据保留，不删除其他任务文件。结构化记录见 [研究授权](4.0l-research-authorization.json)。
