# 4.0k 模拟器功能流程复核

**后续导出修复（2026-09-08）**：用户发现旧复制/PDF 仍包含 MemoryID。现按 ADR-024 将复制、Markdown、PDF 和系统分享统一改为标题与正文，诗歌连续换行；内部来源与交接前授权保留。最终定向回归 71 tests / 11 suites / 0 failures，两台正常 App 剪贴板及实际 PDF 提取文本一致且不含附加来源标记。修复版已安装，详见 [导出修复证据](4.0k-clean-export-results.json)。下文早期“剪贴板含引用标记”的观察是历史记录，已由本修复取代。


用户决定（2026-09-08）：“先不需要关心模型生成内容的质量问题，现在要紧的是功能流程的打通”。当前优先级改为模拟器功能闭环；内容质量调优、新模型下载研究和实机评测暂缓。此决定不表示质量已通过，不删除既有失败断言，也不改变最终发布门禁。

## 本轮结果

| 流程 | 结果与证据边界 |
| --- | --- |
| 搜索 → AI 创作 → 诗歌 → 生成 | 17 Pro 正常 App 本轮重新生成四行英文诗；16 Pro 保留上一轮正常生成的四行中文诗。两台完整生成及来源跳转已有 v12 记录，本轮没有重跑 16 Pro 生成。 |
| 复制 | 两台均点击复制后通过各自模拟器剪贴板核验正文及引用标记。导出内容仍使用既有逐段 MemoryID 标记；页面显示一个合并的可读来源链接。 |
| Markdown / PDF | 两台均从导出选择器触发对应系统分享；生产数据库各记录一条该格式的 `sharePresented=true`。 |
| 保存到备忘录 / 分享打印 | 两台均触发系统分享；各新增两条 `plainText` 真实呈现记录。没有选择接收目标，不宣称已保存 Notes 或完成打印。 |
| 分享关闭与返回 | 每次用 Escape 关闭系统面板，原诗歌和操作按钮恢复，无生成结果丢失或错误状态。 |
| 异常、引用、取消/恢复、发布 | 本轮定向功能回归 58 tests / 9 suites / 0 failures，1.768s，包含 CreationViewModel、GroundedCitationShare、CreationSourceText、GenerationReplay、GenerationPublication、OfflineGenerationRuntime、CreationTemplate、CreationCitationPresentation、GenerationReferenceMap。 |
| 月度/年度真实分层闭环 | 沿用已执行的真实 E5 摄入、Qwen、月/年 publication 证据，见 `4.0k-simulator-e2e-results.json` 及 production validation；本轮不重复昂贵模型推理。 |

自动化的 runtime semantic snapshot 只能读取 App 内的分享容器，无法读取远程系统分享服务内部控件。真实 `viewDidAppear` 审计、面板关闭前后层级变化与结果保留共同支持呈现结论；不作为目标 App 保存结果证据。未生成截图/视频，未直接写数据库，未引入 fixture 生成器。

回归命令使用 iPhone 17 Pro、Debug `-O`、`-parallel-testing-enabled NO`，日志 `/tmp/echo-4.0k-functional-priority.log`。结果包 `DerivedData/Logs/Test/Test-Echo-2026.09.08_19-00-26--0500.xcresult`。本轮没有修改生产代码、模型、测试断言或验收阈值。已知真实诗歌质量断言仍失败，不包含在这次功能定向测试中；不得将这里的 58 项通过表述为全量测试通过。

机器可读摘要：[4.0k-functional-flow-results.json](4.0k-functional-flow-results.json)。17 Pro 测试完成后已重新启动正常 App；16 Pro 留在中文诗歌结果页。

## Live 审查路径

搜索 `Blue circle test` → 长按首个 89% 结果 → AI 创作 → 诗歌 → 向上滚动露出“生成” → 生成。结果页可点击来源、复制、导出 PDF/Markdown、分享/打印及保存到备忘录。备忘录入口打开系统分享，由用户自行选择目标。

当前检查范围内未发现新增功能阻断。内容质量、完整覆盖率与实机/Release 资格仍待后续验收；Task 4.0k 保持 `in_progress`。

## 照片素材范围澄清（ADR-025）

本记录中的照片先由用户填写了文字描述，因此不构成“新照片无需手写即可 AI 创作”的证据。该缺口是功能输入链路缺失，已建立 4.0l；不是仅需调优模型文采。既有复制/分享与文本生成证据仍有效，照片直接创作产品验收尚未通过。
